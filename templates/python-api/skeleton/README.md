# ${{ values.name }}

${{ values.description }}

## Ownership

| Field | Value |
|---|---|
| Owner | `${{ values.owner }}` |
| System | `${{ values.system }}` |
| Lifecycle | `${{ values.lifecycle }}` |

## Run Locally

Create a virtual environment:

```bash
python -m venv .venv